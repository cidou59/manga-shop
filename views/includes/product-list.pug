if products && products.length
    .row
        each product in products
            .col-md-4.mb-4
                .card.product-element
                    a(href=`/products/${product._id}`)
                      img(src=product.imageUrl, alt=product.name, style="max-width: 50%; height: auto; object-fit: cover; border-radius: 8px;")
                    .card-body
                        h2.card-title= product.name
                        p.card-text Price: €#{product.price}
else
    p No products found.